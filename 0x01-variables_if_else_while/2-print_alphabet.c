#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
*main - prints the alphabet in lowercase
*Return: 0
*/
int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; ++alpha)
putchar(alpha);
return (0);

}
